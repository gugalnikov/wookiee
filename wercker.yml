
box: 
  id: pjvds/scala
  username: $USERNAME # Registry username  
  password: $PASSWORD # Registry password
  tag:latest
build:
  steps:
    - script:
        name: compile
        code: sbt clean compile stage
    - script:
        name: test
        code: sbt test
